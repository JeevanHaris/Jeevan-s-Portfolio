version: '3.8'

services:
  backend:
    build: .
    container_name: Jeevan_portfolio_backend
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=production
      - PORT=8000
    restart: unless-stopped
